export default function ClearingAgentEntryPage() {
  return (
    <div className="space-y-4">
      <div>
        <h1 className="text-2xl font-semibold tracking-tight">Clearing Agent</h1>
        <p className="text-sm text-muted-foreground">
          Clearing agent forms (customs, duty, bills, documents) will be available here.
        </p>
      </div>
      <section className="rounded-lg border bg-card p-5 text-sm text-muted-foreground">
        Placeholder screen (UI foundation). Approval workflow and audit logs will attach when APIs are enabled.
      </section>
    </div>
  );
}

