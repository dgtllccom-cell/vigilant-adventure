import { LocationManagementWorkspace } from "@/features/locations/components/location-management-workspace";

export default function LocationsSettingsPage() {
  return (
    <div className="w-full h-full bg-slate-50/50 min-h-screen">
      <LocationManagementWorkspace />
    </div>
  );
}
